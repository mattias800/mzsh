# Docker Compose helpers
alias dcdu='docker compose down && docker compose up --build'
alias dcdua='docker compose down && docker compose --profile all up --build'
alias dcua='docker compose --profile all up --build'
