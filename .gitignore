# Local overrides (never commit machine-specific tweaks)
local.zsh
# Secrets manifest and generated env files (avoid accidental commit)
secrets.manifest.json
secrets.env
secrets.zsh

# IDE
.idea/

# Node tool caches (defensive)
.npm/
.pnpm-store/

# macOS fluff
.DS_Store

# Zsh compiled files
*.zwc
