# AI coding CLIs — skip interactive permission prompts
# clauded: Claude Code without per-toolcall approval
alias clauded='claude --dangerously-skip-permissions'
# codexd: Codex with full access and no approval prompts
alias codexd='codex -s danger-full-access -a never'
